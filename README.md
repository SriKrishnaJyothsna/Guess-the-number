# 🎮 Number Guessing Game

A simple Python console-based game where the computer randomly selects a number between **1 and 100**, and the player tries to guess it. The program provides hints ("Too low!" / "Too high!") until the correct number is guessed.

---

## 📌 Features
- Randomly generates a secret number between 1 and 100.
- Accepts user input and validates it.
- Provides feedback for each guess:
  - Too low
  - Too high
  - Correct guess 🎉
- Tracks the number of attempts taken to guess correctly.
- Handles invalid inputs gracefully.

---

## 🚀 How to Run
1. Make sure you have **Python 3** installed on your system.
2. Save the script as `number_guessing_game.py`.
3. Open a terminal or command prompt.
4. Run the program with:

   ```bash
   python number_guessing_game.py
